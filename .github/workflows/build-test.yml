name: Build and Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build-status: ${{ job.status }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          cmake \
          g++ \
          make

    - name: Configure CMake
      run: |
        mkdir -p build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=Debug

    - name: Build
      run: |
        cd build
        make -j$(nproc)

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: |
          build/
          !build/**/*.o
          !build/**/*.tmp
        retention-days: 1

  test:
    needs: build
    runs-on: ubuntu-latest
    if: success()  # Запускаем только если сборка успешна
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-artifacts
        path: build/

    - name: Install dependencies (for tests)
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          cmake \
          g++ \
          make

    - name: Run tests
      run: |
        cd build
        ctest --output-on-failure --verbose

  docker:
    needs: build
    runs-on: ubuntu-latest
    if: success() && github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-artifacts
        path: build/

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build Docker image
      run: |
        docker build -t system-monitor:${{ github.sha }} .

    - name: Test Docker image
      run: |
        docker run --rm system-monitor:${{ github.sha }} --help

    # - name: Save Docker image (optional)
    #   run: |
    #     docker save system-monitor:${{ github.sha }} | gzip > system-monitor.tar.gz

    # - name: Upload Docker image (optional)
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: docker-image
    #     path: system-monitor.tar.gz
    #     retention-days: 7